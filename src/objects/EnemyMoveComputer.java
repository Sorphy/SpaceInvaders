package objects;

import java.util.ArrayList;

import gameLogic.GameWindow;

public class EnemyMoveComputer {
	
/*	private ArrayList<IMovable> objects;
	private GameWindow window;	
	
	public EnemyMoveComputer(ArrayList<IMovable> objects, GameWindow window) {
		this.objects = objects;
		this.window = window;
	}
	
	private boolean verifyDirection() {
		boolean border = true;
		for(IMovable object : this.objects) {
			if(object instanceof EnemyShip) {
				border = (((EnemyShip) object).checkBorders(this.window.getWindowWidth(), 
						this.window.getWindowHeight(), this.offsetX, this.offsetY));
				if(!border) { break; }
				
			}
		}
		return border;
	}

	public void move() {
		
	}*/

}
